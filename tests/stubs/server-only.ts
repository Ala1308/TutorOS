// Empty stub: replaces the `server-only` module under vitest, since the real
// module is provided by the Next.js bundler at build time and does not exist
// at runtime. Server-only enforcement still happens during `next build`.
export {};
